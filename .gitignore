node_modules/
dist/
build/
.env
.env.local
*.log
server/data/
.DS_Store
coverage/
.vite/
.claude/
CLAUDE.md
# Secrets / key files (real .env is already ignored above; .env.example is committed)
*.pem
*.key
*.p12
*.keystore

# Design working material (reference bundles; production assets are copied into
# web/public/ and extension/ — the bundle itself stays local)
design_handoff_console_v2/
