export default {
  plugins: {
    // Tailwind v4: the PostCSS plugin moved to its own package and handles
    // imports + vendor prefixing internally (autoprefixer no longer needed).
    "@tailwindcss/postcss": {},
  },
};
