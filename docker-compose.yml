services:
  server:
    build: ./server
    env_file: ./server/.env
    environment:
      - WEB_ORIGIN=http://localhost:8080
      - APP_BASE_URL=http://localhost:8080
      - DATA_DIR=/data
    volumes:
      - trust-data:/data
    ports:
      - "8787:8787"

  web:
    build: ./web
    depends_on:
      - server
    ports:
      - "8080:80"

volumes:
  trust-data:
